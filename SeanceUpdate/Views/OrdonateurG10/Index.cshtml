@model IEnumerable<SeanceUpdate.Models.OrdonateurG10>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrdNom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrdPrenom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrdFonction)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrdNom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrdPrenom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrdFonction)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OrdCode">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OrdCode">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OrdCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
